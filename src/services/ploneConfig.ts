/**
 * URLs do site Plone UFAC (proxy same-origin evita CORS do Varnish).
 */
export const SITE_URL = 'https://www3.ufac.br';

/** API via proxy (/__plone__ no Vite e no nginx) */
export const BASE_URL = '/__plone__/++api++';
